node_modules/
dist/
*.log
.env
.dev.vars
.wrangler/

# art/ and data/ are generated: sync.sh --extension downscales the card
# screenshots from the game repo into them. They ARE committed (GitHub Pages
# serves them from the repo root), but are never hand-edited.
